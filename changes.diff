Information_Security\.vscode\settings.json--- old+++ new@@ -1,7 +1,7 @@ {
     "workbench.colorCustomizations": {
         "activityBar.background": "#01351F",
-        "titleBar.activeBackground": "#63f1b4",
+        "titleBar.activeBackground": "#014B2B",
         "titleBar.activeForeground": "#E8FFF5"
     },
     "files.associations": {
Information_Security\diffie.cpp--- old+++ new@@ -21,13 +21,32 @@ 
 int main()
 {
-    ll xa, ya, xb, yb, alfa= 3, ka, kb;     
+    ll xa, ya, xb, yb, alfa= 9, ka, kb;     
     /*  
         xa, xb-> private keys for a, b
         ya, yb-> public keys for a,b
         ka, kb-> calculated secret key
         alfa-> premitive root of the given prime number
     */
+
+    input:
+    cout<< "Enter value of alfa: ";
+    cin>> alfa;
+
+    bool prr[prm];
+    for(int i=0; i<prm; i++) prr[i]= 0;
+    for(int i=0; i<prm-1; i++)
+        prr[pow_a_b(alfa, i, prm)]=1;
+
+    for(int i=1; i<prm; i++) 
+        if(!prr[i]) 
+        {
+            cout<< i<< '\n';
+            cout<< "alfa is not a premitive root of "<< prm<< '\n';
+            // return 0;
+            goto input;
+        }
+
     cout<< "Enter chosen private key for A: ";
     cin>> xa;
     cout<< "Enter chosen private key for B: ";
Information_Security\hashVal.cppInformation_Security\README.mdInformation_Security\SHA512.cppInformation_Security\_sha512.cppInformation_Security\_sha512.hInformation_Security\.vscode\settings.json--- old+++ new@@ -1,7 +1,7 @@ {
     "workbench.colorCustomizations": {
         "activityBar.background": "#01351F",
-        "titleBar.activeBackground": "#63f1b4",
+        "titleBar.activeBackground": "#014B2B",
         "titleBar.activeForeground": "#E8FFF5"
     },
     "files.associations": {
Information_Security\diffie.cpp--- old+++ new@@ -21,13 +21,32 @@ 
 int main()
 {
-    ll xa, ya, xb, yb, alfa= 3, ka, kb;     
+    ll xa, ya, xb, yb, alfa= 9, ka, kb;     
     /*  
         xa, xb-> private keys for a, b
         ya, yb-> public keys for a,b
         ka, kb-> calculated secret key
         alfa-> premitive root of the given prime number
     */
+
+    input:
+    cout<< "Enter value of alfa: ";
+    cin>> alfa;
+
+    bool prr[prm];
+    for(int i=0; i<prm; i++) prr[i]= 0;
+    for(int i=0; i<prm-1; i++)
+        prr[pow_a_b(alfa, i, prm)]=1;
+
+    for(int i=1; i<prm; i++) 
+        if(!prr[i]) 
+        {
+            cout<< i<< '\n';
+            cout<< "alfa is not a premitive root of "<< prm<< '\n';
+            // return 0;
+            goto input;
+        }
+
     cout<< "Enter chosen private key for A: ";
     cin>> xa;
     cout<< "Enter chosen private key for B: ";
Information_Security\hashVal.cppInformation_Security\README.mdInformation_Security\SHA512.cppInformation_Security\_sha512.cppInformation_Security\_sha512.h